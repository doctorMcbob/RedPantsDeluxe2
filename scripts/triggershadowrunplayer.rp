TRIGGER|0,0,0,0|False|
START:0 empty 0,0
LOOK:0 empty 0,0
END:0 empty 0,0

|START:0|
set self state LOOK
set self frame 0

|LOOK:0|
set self frame 0

|END:0|
goodbye

|COLLIDE|
if player in self.name
   set shadow0 mode RUN
   set related state END
endif

