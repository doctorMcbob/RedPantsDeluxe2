MGMT|0,0,0,0|False|
START:0 empty 0,0

|START:0|
if COLLIDE?
   if yougotthistext0.state != SHOW
      set yougotthistext0 state SHOW
      set yougotthistext0 frame 0
   endif
   break
endif
if yougotthistext0.state == SHOW
   set yougotthistext0 state OFF
   set yougotthistext0 frame 0   
endif
