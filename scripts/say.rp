SAY|0,0,0,0|False|
START:0 empty 0,0
SAY:0 empty 0,0

|START:0|
set self message "WELCOME TO RED PANTS DELUXE 2"
set self state SAY
set self frame -1
set self timer 120

|SAY:0|
set self calc len self.message - 1

if self.calc == self.frame
   goodbye
endif

create letter0 self.name + letter + self.frame 16 * self.frame + self.x self.y
set self.name + letter + self.frame letter self.message at self.frame
if self.timer != -1
   set self.name + letter + self.frame timer 120
endif

