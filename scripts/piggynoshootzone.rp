ZONE|0,0,0,0|False|
START:0 empty 0,0
WAIT:0 empty 0,0

|START:0|
set self state WAIT

|COLLIDE|
if self.ispig
   if self.state == STAND
      set self state SCARE
      set self frame 0
   endif
endif
